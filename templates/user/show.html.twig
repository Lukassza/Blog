{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
{% include "Header.php" %}

<div class="card mb-3">
  <h3 class="card-header">Profil de {{ app.user.pseudo }}</h3>
  <div class="card-body">
    
  </div>
  {# <svg xmlns="http://www.w3.org/2000/svg" class="d-block user-select-none" width="100%" height="200" aria-label="Placeholder: Hoho pas d'image" focusable="false" role="img" preserveAspectRatio="xMidYMid slice" viewBox="0 0 318 180" style="font-size:1.125rem;text-anchor:middle">
    <rect width="100%" height="100%" fill="#868e96"></rect>
    <text x="50%" y="50%" fill="#dee2e6" dy=".3em"></text>
  </svg> #}
  <img style="max-width:35vw; max-heigth:35vh;" src="{{app.user.photo}}"/>
  <div class="card-body">
  </div>
  <ul class="list-group list-group-flush">
    <li class="list-group-item">{{ app.user.pseudo }}</li>
    <li class="list-group-item">{{ app.user.email }}</li>
    
  </ul>
  <div class="card-body">
    <a href="{{ path('app_user_edit', {'id': user.id}) }}">Modifier</a>
    {{ include('user/_delete_form.html.twig') }}
  </div>
  
</div>



    {# <table class="table">
        <tbody>
            <tr>
                <th>Email</th>
                <td>{{ app.user.email }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ user.photo }}</td>
            </tr>
            <tr>
                <th>Pseudo</th>
                <td>{{ app.user.pseudo }}</td>
            </tr>
        </tbody>
    </table> #}

    
{% endblock %}
